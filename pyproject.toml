[project]
name = "smf"
version = "0.1.0"
description = "Social Media Factory — orquestador multicanal ligero"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
include = ["smf*"]
